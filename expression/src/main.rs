fn main() {
    // statement

    // expression
    // blocks are expressions, so the can be used as values in assignments;
    // the last expression in block wiil be assigned.
    let z = { 2 * 2 };
    println!("{}", z);
}
