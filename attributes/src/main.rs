fn main() {
    // An attribute is metadata applied to some module, crete or item.
    // can be used to/for:
    // 1. conditional compliation of code
    // 2. set create name, version, and type(binary or library)
    // 3. disable lints
    // 4. enable compiler features(macros, glob import, etc)
    // 5. link to a foregin library
    // 6. mark functions as unit test
    // 7. mark functions that will be part of benchmark
    // 8. attribute like macros
}
